# 앱 Pod 전용 서비스 어카운트, IRSA로 S3 읽기 권한 해당 SA만 부여
# arn:aws:iam::<계정ID>:role/health-record-app은 terraform output app_irsa_role_arn 값 치환
apiVersion: v1
kind: ServiceAccount
metadata:
  name: health-record-app
  namespace: health-record
  annotations:
    eks.amazonaws.com/role-arn: <APP_ROLE_ARN>
